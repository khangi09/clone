version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:22.15
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm ci
      - run:
          name: Install SonarScanner
          command: |
            npm install sonar-scanner --save-dev
            chmod +x ./node_modules/sonar-scanner/bin/sonar-scanner
      - run:
          name: Run Tests
          command: npm test || echo "No tests found"
      - run:
          name: 🚀 Run SonarCloud Analysis
          command: |
            ./node_modules/sonar-scanner/bin/sonar-scanner \
              -Dsonar.projectKey=backend \
              -Dsonar.organization=khangi09 \
              -Dsonar.sources=. \
              -Dsonar.host.url=https://sonarcloud.io \
              -Dsonar.login=squ_2f16ab24463726f6a5ff7d1115fcbdd50df0dc4d

workflows:
  version: 2
  build_and_scan:
    jobs:
      - build

